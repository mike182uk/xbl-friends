#!/usr/bin/env sh

rm -rf ./dist/*

electron-packager ./build \
  --platform darwin \
  --arch x64 \
  --out ./dist \
  --icon ./build/electron/xbl-friends.icns \
  --asar

cd ./dist

for dir in *
do
  tar -cjf ${dir}-${TRAVIS_TAG}.tar.bz2 ${dir}
done
